import { BillingService } from './billing.service';
describe('BillingService webhook ordering',()=>{it('does not let an older subscription event overwrite newer state',async()=>{const subscriptions={findOne:jest.fn().mockResolvedValue({id:'s',stripeSubscriptionId:'sub_test',status:'active',lastStripeEventCreated:'200'}),save:jest.fn()};const catalog={priceByStripeId:jest.fn()};const service=new BillingService({get:jest.fn()} as any,{} as any,{} as any,catalog as any,{} as any,{} as any,subscriptions as any,{} as any,{} as any,{} as any,{} as any);const applied=await(service as any).syncSubscription({id:'sub_test',metadata:{businessId:'b'},items:{data:[{price:{id:'price_old',recurring:{interval:'month'}}}]}},'b',100);expect(applied).toBe(false);expect(subscriptions.save).not.toHaveBeenCalled();});});
